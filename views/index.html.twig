<!DOCTYPE html>
<html>
  <head>
    {% include '/includes/head.html.twig' %}
  </head>
  <body>
    {% include '/includes/navbar.html.twig' %}
    <div class="container">
      <h1>Hair Salon App</h1>
      <p>Below is a list of our wonderful hair stylists!</p>

      {% if stylists is not empty %}
        <ul>
          {% for stylist in stylists %}
            <li class="well"><a href="#">{{ stylist.getName }}</a></li>
          {% endfor %}
        </ul>
      {% else %}
        <div class="well">
          <h2>We currently have no stylists!</h2>
        </div>
      {% endif %}

      {% include '/includes/message.html.twig'%}
      <h2>Add a stylist:</h2>
      <form action='/addStylist' method='post'>
        <div class="form-group">
          <label for='stylist-name'>Stylist Name</label>
          <input name='stylist-name' type='text' class='form-control'>
        </div>
        <button type='submit' class='btn btn-success'>Submit</button>
      </form>

    </div>
  </body>
</html>
